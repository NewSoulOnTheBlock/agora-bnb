node_modules/
dist/
.vercel
*.tsbuildinfo

# Hardhat build output
artifacts/
cache/
typechain-types/

# Never commit secrets. .env.local here holds a Vercel OIDC token.
.env
.env.*
!.env.example

.DS_Store
*.log

# A stray nested clone of this same repo. Its work is already merged
# (3a79bec is an ancestor of main); it is redundant, not a submodule.
contracts/tithe/
